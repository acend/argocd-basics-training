[build]
  publish = "public"
  functions = "functions"

[build.environment]
  NODE_VERSION = "22.21.1"
  NPM_VERSION = "11.6.2"

[context.production]
  command = "npm run build -- --environment $TRAINING_HUGO_ENV"

[context.deploy-preview]
  command = "npm run build -- -b $DEPLOY_PRIME_URL --environment $TRAINING_HUGO_ENV"

[context.branch-deploy]
  command = "npm run build -- -b $DEPLOY_PRIME_URL --environment $TRAINING_HUGO_ENV"

[context.next]
  command = "npm run build"

[context.next.environment]
  HUGO_ENV = "next"

[dev]
  framework = "#custom"
  command = "npm run start"
  targetPort = 1313
  port = 8888
  publish = "public"
  autoLaunch = false

[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "connect-src * 'self';"
